{
  "campaign": "cycle3-2026-08",
  "status": "AUTHORED for Cycle 3 Stage B (frontier catch-up, model-roster-process.md §3). STOP 1 spend approved by Matt (2026-08-10, $25/stage). Ride Stage B — no separate run.",
  "created": "2026-08-11",
  "source": "model-roster-process.md §3 (immediate catch-up) + roster-sweep candidates-2026-08.json (all listed + priced + viable). Adapter callability pre-flight-verified 2026-08-11 (chat.completions / messages).",
  "pricing_provenance": {
    "source": "data/pricing-map.json (pinned LiteLLM snapshot); resolved through the adapter prefix rules",
    "as_of": "2026-08-11 (roster-sweep verification run)",
    "url": "https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json",
    "note": "Each target's per-token price + capability flags come from the snapshot; the declared layer is generated by registry/build-declared.ts from that plus the adapter static capability maps (same mechanism as the 2026-08-02 sonnet-5/haiku enrollment). callNotes: the openai targets inherit OPENAI_COMMON + flex-eligibility from call-notes.ts automatically; anthropic targets carry no model-specific notes (matching the 2026-08-02 pattern)."
  },
  "targets": [
    {
      "provider": "openai",
      "model": "gpt-5.5",
      "family": "GPT-5.5",
      "pricing_usd_per_mtok": { "input": 5, "output": 30 },
      "adapter": "chat.completions (verified 2026-08-11: finish=stop, reasoning_tokens billed as output)"
    },
    {
      "provider": "openai",
      "model": "gpt-5.4",
      "family": "GPT-5.4",
      "pricing_usd_per_mtok": { "input": 2.5, "output": 15 },
      "adapter": "chat.completions (verified 2026-08-11)"
    },
    {
      "provider": "anthropic",
      "model": "claude-opus-5",
      "family": "Claude Opus 5",
      "pricing_usd_per_mtok": { "input": 5, "output": 25 },
      "adapter": "messages (verified 2026-08-11: stop_reason=end_turn, provider-default thinking — no thinkingConfig sent)"
    },
    {
      "provider": "anthropic",
      "model": "claude-fable-5",
      "family": "Claude Fable 5",
      "pricing_usd_per_mtok": { "input": 10, "output": 50 },
      "adapter": "messages (verified 2026-08-11)"
    }
  ],
  "deferred": [
    {
      "target": "openai/gpt-5.5-pro",
      "reason": "FAIL-CLOSED (adapter-support gate). Listed + priced ($30/$180 per Mtok), but it is a Responses-API model: v1/chat/completions returns 'This is not a chat model and thus not supported in the v1/chat/completions endpoint. Did you mean to use v1/completions?'. The probe openai caller (src/vendor/openai.ts) is chat.completions-only. Enrolling it would burn its costly envelope on failed calls. FLAGGED FOR ARCHITECT: needs a Responses-API probe adapter before it can be measured.",
      "flagged_for_architect": true
    },
    {
      "target": "openai/gpt-5.5-pro (cost note)",
      "reason": "Even with a Responses adapter, at $180/Mtok output a thinking-heavy Pro model could consume its whole per-model envelope on ~1 fixture — size the envelope/ceiling deliberately when it is enrolled (model-roster-process §3 anticipated 5.5-pro dominating spend)."
    }
  ],
  "runbook": [
    "Ride Cycle 3 Stage B: these targets are added to the cycle plan alongside the existing registry re-run. The estimate-only gate reports the projected total BEFORE any spend; if it breaches the $25 ceiling the builder STOPS and asks (model-roster-process §3) rather than trimming coverage.",
    "Fail-closed on unpriced (P6) and unreachable (no key) is enforced by the cycle runner; these 4 are priced + reachable + adapter-verified.",
    "After the run, build-declared picks up the new result files and generates each declared entry; verify the 4 appear in registry.json with pricing + flags."
  ]
}
